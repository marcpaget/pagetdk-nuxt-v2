export {}

// Add and export your tables here, for example:
// import { pgTable, text, uuid } from 'drizzle-orm/pg-core'
//
// export const profiles = pgTable('profiles', {
//   id: uuid().defaultRandom().primaryKey(),
//   fullName: text('full_name'),
// })
